# Atlas

Tax integrity. Same canonical model, same Foundation, different rules.

Do not start this app until Pulse has run on one real (uncommitted) customer file.

Rules will live under `packages/rules` as `ATLAS_*` and seed from here, the way Pulse seeds `DUP_*`.
